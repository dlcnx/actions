name: Update

on:
  workflow_dispatch:
  push:
    branches: [ "master" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Shell
      run: |
        echo "start!"
        
    - name: key
      run: |
        echo ${{ shell.KEY }}

    - name: Finish
      run: |
        echo "finish!"
